<view class="reco-list">
  <view
    wx:for="{{items}}"
    wx:key="listKey"
    class="reco-list-item"
    data-id="{{item.id}}"
    bindtap="onItemTap"
  >
    <recommended-card item="{{item}}" layout="list" compact="{{compact}}" inverse="{{inverse}}" />
  </view>
</view>
