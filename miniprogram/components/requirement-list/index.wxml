<view class="wrap">
  <text class="kicker">资格条件</text>
  <view class="row" wx:for="{{items}}" wx:key="id">
    <text class="mark mark-{{item.status}}">{{item.mark}}</text>
    <text class="label">{{item.label}}</text>
  </view>
</view>
